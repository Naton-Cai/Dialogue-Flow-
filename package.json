{
  "name": "dialogflowFirebaseFulfillment",
  "description": "This is the default fulfillment for a Dialogflow agent using Cloud Functions for Firebase",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "Google Inc.",

  "engines": {
    "node": "20"
  },

  "scripts": {
    "start": "firebase emulators:start --only functions:dialogflowFirebaseFulfillment",
    "serve": "firebase emulators:start --only functions:dialogflowFirebaseFulfillment"
  },

  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.8.0",
    "dialogflow-fulfillment": "^0.6.1",
    "fast-fuzzy": "^1.12.0"
  }
}